#!/usr/bin/make -f

export DH_VERBOSE = 1
export PYBUILD_NAME = alpha

%:
	dh $@ --with python3 --buildsystem=meson

override_dh_auto_install:
	dh_auto_install
	find debian/alpha -type d -name __pycache__ -exec rm -rf {} +
