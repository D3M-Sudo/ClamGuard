name: Debian Build
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: sudo apt-get update
      - run: sudo apt-get install -y build-essential devscripts equivs dh-python
      - run: sudo mk-build-deps -i -r
      - run: dpkg-buildpackage -us -uc -b
      - run: cp ../*.deb .
      - uses: actions/upload-artifact@v7
        with:
          name: clamguard-deb
          path: ./*.deb
